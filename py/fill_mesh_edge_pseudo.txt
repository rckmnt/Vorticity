

C:\Users\Scott\Dropbox\NBI\Vorticity\OUTPUT\samples_to_test_capping

for very file in files:

    import mesh

        sel all
        if mesh is CLOSED:
            sel all
            delete
        else:
            ALLOW USER CONTROL

            Fill mesh edge

            CONT

            if CLOSED
                export as SAME NAME

            else

                Fill Mesh Edge
